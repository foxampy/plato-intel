name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - 'main/**'
      - 'v1/**'
      - 'v2/**'
      - 'transformation.html'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: main/package-lock.json

      - name: Install dependencies
        run: cd main && npm ci

      - name: Build main site
        run: cd main && npm run build

      - name: Prepare deploy folder
        run: |
          mkdir -p deploy
          cp -r main/dist/* deploy/
          cp -r v1 deploy/v1
          cp -r v2 deploy/v2
          if [ -f transformation.html ]; then cp transformation.html deploy/; fi

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./deploy

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
